package com.akabex86.commands;

public class CommandMail {
    //todo brief system mit buecher inventaren und item attachment support (kitsystem backend mit auto delete after read)
    //gespeichtert in einer seperaten mails.yml
    //mit portio und mmo like system
    //befehle & sub commands
    //ALL THESE ARE SOLVED TIH CLICKABLE TEXT
    // /mail <create,attach,read,delete> <title>
    // /mail send <id> <receiver> (players with permission survival.mail.everyone can do @everyone to send a mail to all registered players.)
    // /mail list <all,inbox,sent,unread,read> <index(1-99)>
    // Contents of a "Mail" Object: Mail(int ID,String sender,String[] receiver(s),String title,String Message,Itemstack[] Attachments);

}
